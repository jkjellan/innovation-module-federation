import React from 'react';
import { shallow } from 'enzyme';
import { expect } from 'chai';
//import HeaderComponent from '../Header';

describe('Header', () => {
  it('Correctly renders the header', () => {

    /* Need to implement mocking for getParams()
    const headerWrapper = shallow(<HeaderComponent/>);

    headerWrapper.instance().getParams = () => {
      return { householdName: "Yihun" };
    }

    expect(headerWrapper.find('#application-header-name').length).to.equal(1);
    expect(headerWrapper.find('#application-header-name')[0].text()).to.equal('Cross-sell & upsell opportunities');  
    */
   expect(true).to.equal(true);  
  });
});
